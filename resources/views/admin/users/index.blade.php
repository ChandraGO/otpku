@extends('layouts.app')
@php($title = 'Pengguna')
@section('content')
<div><h1 class="text-3xl font-black tracking-tight">Pengguna & saldo</h1><p class="mt-2 text-sm text-slate-500">Cari akun, tinjau status, dan buka ledger keuangan.</p></div><form class="card mt-6 flex gap-3 p-4" method="get"><input class="input" name="q" value="{{ request('q') }}" placeholder="Username, email, atau WhatsApp"><button class="btn-primary">Cari</button></form><div class="mt-6 table-wrap"><table class="table"><thead><tr><th>User</th><th>WhatsApp</th><th>Status</th><th>Saldo</th><th>Terdaftar</th><th></th></tr></thead><tbody>@forelse($users as $user)<tr><td><div class="font-semibold">{{ $user->username }}</div><div class="text-xs text-slate-500">{{ $user->email }}</div></td><td>{{ $user->whatsapp }}</td><td><x-status :value="$user->status" /></td><td class="font-bold">Rp {{ number_format((float)$user->balance,0,',','.') }}</td><td>{{ $user->created_at->format('d M Y') }}</td><td><a class="btn-secondary !px-3 !py-2" href="{{ route('admin.users.show',$user) }}">Kelola</a></td></tr>@empty<tr><td colspan="6" class="py-10 text-center text-slate-500">Pengguna tidak ditemukan.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $users->links() }}</div>
@endsection
