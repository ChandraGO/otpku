@extends('layouts.app')
@php($title = 'Pembayaran')
@section('content')
<div><h1 class="text-3xl font-black">Pembayaran top up</h1><p class="mt-2 text-sm text-slate-500">Verifikasi status Pakasir dan audit pengkreditan saldo.</p></div><form class="card mt-6 flex gap-3 p-4" method="get"><select class="input" name="status"><option value="">Semua status</option>@foreach(['creating','pending','completed','expired','cancelled','failed'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ $status }}</option>@endforeach</select><button class="btn-primary">Filter</button></form><div class="mt-6 table-wrap"><table class="table"><thead><tr><th>Order</th><th>User</th><th>Nominal</th><th>Metode</th><th>Status</th><th>Waktu</th><th></th></tr></thead><tbody>@forelse($topups as $topup)<tr><td>{{ $topup->order_id }}</td><td>{{ $topup->user?->email }}</td><td>Rp {{ number_format((float)$topup->amount,0,',','.') }}</td><td>{{ strtoupper(str_replace('_',' ',$topup->payment_method)) }}</td><td><x-status :value="$topup->status" /></td><td>{{ $topup->created_at->format('d M H:i') }}</td><td><a class="btn-secondary !px-3 !py-2" href="{{ route('admin.topups.show',$topup) }}">Detail</a></td></tr>@empty<tr><td colspan="7" class="py-10 text-center">Tidak ada top up.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $topups->links() }}</div>
@endsection
