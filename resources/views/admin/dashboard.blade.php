@extends('layouts.app')
@php($title = 'Admin Dashboard')
@section('content')
<div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm text-brand-600 dark:text-brand-300">Administrator</p><h1 class="text-3xl font-black tracking-tight">Ringkasan operasional</h1></div><a class="btn-secondary" href="{{ route('admin.settings.index') }}">Buka pengaturan</a></div>
<div class="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-6">@foreach([['Pengguna',$stats['users']],['Saldo user','Rp '.number_format($stats['user_balance'],0,',','.')],['Top up berhasil','Rp '.number_format($stats['completed_topups'],0,',','.')],['Order hari ini',$stats['orders_today']],['Penjualan hari ini','Rp '.number_format($stats['revenue_today'],0,',','.')],['Profit hari ini','Rp '.number_format($stats['profit_today'],0,',','.')]] as $stat)<div class="stat"><div class="text-xs text-slate-500">{{ $stat[0] }}</div><div class="mt-2 text-xl font-black">{{ $stat[1] }}</div></div>@endforeach</div>
<div class="mt-6 card p-5"><div class="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><h2 class="font-bold">Saldo SMS Virtual</h2><p class="mt-1 text-xs text-slate-500">Dibaca langsung dari backend provider.</p></div>@if($providerError)<span class="text-sm text-rose-500">{{ $providerError }}</span>@else<span class="text-2xl font-black text-brand-600 dark:text-brand-300">@if(is_numeric($providerBalance)) Rp {{ number_format((float) $providerBalance, 0, ',', '.') }} @else {{ is_scalar($providerBalance) ? $providerBalance : json_encode($providerBalance) }} @endif</span>@endif</div></div>
<div class="mt-6 grid gap-6 xl:grid-cols-2"><section class="card p-5"><div class="flex justify-between"><h2 class="font-bold">Pesanan terbaru</h2><a class="text-sm text-brand-600 dark:text-brand-300" href="{{ route('admin.orders.index') }}">Semua</a></div><div class="mt-4 space-y-3">@foreach($recentOrders as $order)<a href="{{ route('admin.orders.show',$order) }}" class="flex justify-between gap-3 rounded-xl border border-slate-200 p-3 dark:border-white/10"><div class="min-w-0"><div class="truncate font-semibold">{{ $order->service_name }} · {{ $order->user?->email }}</div><div class="mt-1 text-xs text-slate-500">Rp {{ number_format((float)$order->sell_price,0,',','.') }}</div></div><x-status :value="$order->status" /></a>@endforeach</div></section><section class="card p-5"><div class="flex justify-between"><h2 class="font-bold">Top up terbaru</h2><a class="text-sm text-brand-600 dark:text-brand-300" href="{{ route('admin.topups.index') }}">Semua</a></div><div class="mt-4 space-y-3">@foreach($recentTopups as $topup)<a href="{{ route('admin.topups.show',$topup) }}" class="flex justify-between gap-3 rounded-xl border border-slate-200 p-3 dark:border-white/10"><div class="min-w-0"><div class="truncate font-semibold">{{ $topup->user?->email }}</div><div class="mt-1 text-xs text-slate-500">{{ $topup->order_id }} · Rp {{ number_format((float)$topup->amount,0,',','.') }}</div></div><x-status :value="$topup->status" /></a>@endforeach</div></section></div>
@endsection
